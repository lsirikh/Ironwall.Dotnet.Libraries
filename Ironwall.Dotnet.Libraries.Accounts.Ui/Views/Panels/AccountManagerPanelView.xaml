<UserControl
	x:Class="Ironwall.Dotnet.Libraries.Accounts.Ui.Views.Panels.AccountManagerPanelView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behavior="clr-namespace:Ironwall.Dotnet.Libraries.Utils.Behaviors;assembly=Ironwall.Dotnet.Libraries.Utils"
	xmlns:cal="http://caliburnmicro.com"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:local="clr-namespace:Ironwall.Dotnet.Libraries.Accounts.Ui.Views.Panels"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:sys="clr-namespace:System;assembly=mscorlib"
	xmlns:utilPass="clr-namespace:Ironwall.Dotnet.Libraries.Utils.PasswordControl;assembly=Ironwall.Dotnet.Libraries.Utils"
	xmlns:utils="clr-namespace:Ironwall.Dotnet.Libraries.Utils;assembly=Ironwall.Dotnet.Libraries.Utils"
	FontFamily="{DynamicResource MaterialDesignFont}"
	mc:Ignorable="d">
	<UserControl.Resources>
		<!--  뷰 전체 DataContext(ViewModel)를 상속받아서 Data 에 할당  -->
		<utils:BindingProxy x:Key="proxy" Data="{Binding}" />
	</UserControl.Resources>

	<md:DialogHost
		x:Name="DialogHost"
		DialogTheme="Inherit"
		Identifier="AccountManagerView">

		<md:Card
			x:Name="CardContents"
			Width="800"
			Height="600"
			MinWidth="600"
			MinHeight="400"
			Padding="20"
			VerticalAlignment="Stretch"
			md:ThemeAssist.Theme="Inherit"
			Opacity="0.9"
			UniformCornerRadius="10">
			<Grid HorizontalAlignment="Center">
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<StackPanel Grid.Row="0" Orientation="Vertical">
					<StackPanel
						Grid.Row="0"
						VerticalAlignment="Center"
						Orientation="Horizontal">
						<md:PackIcon Kind="Account" Style="{StaticResource DialogTitleIcon}" />
						<TextBlock Style="{StaticResource DialogTitle}" Text="사용자 정보" />
					</StackPanel>


					<Separator Background="#33000000" Style="{StaticResource MaterialDesignLightSeparator}" />


					<Grid Grid.Row="0" Margin="10,0">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="250" />
							<ColumnDefinition Width="1*" />
							<ColumnDefinition Width="1*" />
							<ColumnDefinition Width="70" />
							<ColumnDefinition Width="10" />
							<ColumnDefinition Width="70" />
							<ColumnDefinition Width="10" />
							<ColumnDefinition Width="70" />
							<ColumnDefinition Width="10" />
							<ColumnDefinition Width="70" />
							<ColumnDefinition Width="10" />
							<ColumnDefinition Width="70" />
							<ColumnDefinition Width="10" />
							<ColumnDefinition Width="70" />
						</Grid.ColumnDefinitions>
						<!--<Grid Grid.Column="0">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="1*" />
								<ColumnDefinition Width="4*" />
							</Grid.ColumnDefinitions>
							<md:PackIcon
								Grid.Column="0"
								Kind="User"
								Style="{StaticResource TitlePackIconStyle}" />

							<TextBlock
								Grid.Column="1"
								Style="{StaticResource TitleTextBlockStyle}"
								Text="고객 정보 리스트" />
						</Grid>-->

						<!--  Add Button  -->
						<Button
							Grid.Column="9"
							IsEnabled="{Binding IsButtonEnable}"
							Style="{StaticResource IconButtonStyle}"
							ToolTip="추가하기">
							<cal:Message.Attach>
								[Event Click] = [Action OnClickInsertButton($source, $eventArgs]
							</cal:Message.Attach>
							<md:PackIcon
								Width="30"
								Height="30"
								Kind="AddBox" />
						</Button>

						<!--  Remove Button  -->
						<Button
							Grid.Column="11"
							IsEnabled="{Binding IsButtonEnable}"
							Style="{StaticResource IconButtonStyle}"
							ToolTip="삭제하기">
							<cal:Message.Attach>
								[Event Click] = [Action OnClickDeleteButton($source, $eventArgs]
							</cal:Message.Attach>
							<md:PackIcon
								Width="30"
								Height="30"
								Kind="MinusBox" />
						</Button>

						<!--  Refresh Button  -->
						<Button
							Grid.Column="13"
							IsEnabled="{Binding IsButtonEnable}"
							Style="{StaticResource IconButtonStyle}"
							ToolTip="갱신하기">
							<cal:Message.Attach>
								[Event Click] = [Action OnClickReloadButton($source, $eventArgs)]
							</cal:Message.Attach>
							<Button.Content>
								<Grid>
									<ProgressBar
										Width="30"
										Height="30"
										IsIndeterminate="True"
										Style="{DynamicResource MaterialDesignCircularProgressBar}"
										Visibility="{Binding ReloadButtonEnable, Converter={StaticResource BoolToInverseVisibleConverter}}"
										Value="44" />
									<md:PackIcon
										Width="30"
										Height="30"
										Kind="CircleArrows"
										ToolTip="갱신하기"
										Visibility="{Binding ReloadButtonEnable, Converter={StaticResource BooleanToVisibilityConverter}}" />
								</Grid>
							</Button.Content>

						</Button>

						<!--  Save Button  -->
						<!--<Button Grid.Column="9"
								VerticalAlignment="Center"
								Margin="2 2"
								Style="{StaticResource MaterialDesignIconButton}"
								ToolTip="저장하기"
								IsEnabled="{Binding IsButtonEnable}"
								md:RippleAssist.IsCentered="True">
							<cal:Message.Attach>
								[Event Click] = [Action OnClickSaveButton($source, $eventArgs]
							</cal:Message.Attach>
							<Button.Content>
								<Grid>
									<ProgressBar Width="30"
												 Height="30"
												 Visibility="{Binding SaveButtonEnable, Converter={StaticResource BoolToInverseVisibleConverter}}"
												 Style="{DynamicResource MaterialDesignCircularProgressBar}"
												 IsIndeterminate="True"
												 Value="44" />
									<md:PackIcon Kind="FloppyDisc"
												 Width="30" Height="30"
												 Visibility="{Binding SaveButtonEnable, Converter={StaticResource BooleanToVisibilityConverter}}"
												 ToolTip="저장하기"/>
								</Grid>
							</Button.Content>
						</Button>-->
					</Grid>
				</StackPanel>
				<Grid
					Grid.Row="1"
					Width="800"
					Height="300">
					<ProgressBar
						Width="100"
						Height="20"
						IsIndeterminate="True"
						Style="{StaticResource MaterialDesignCircularProgressBar}"
						Visibility="{Binding IsVisible, Converter={StaticResource BoolToInverseVisibleConverter}}" />
				</Grid>
				<DataGrid
					x:Name="DataGridUsers"
					Grid.Row="1"
					cal:Message.Attach="[Event MouseDoubleClick] = [Action OnClickAccountDetail($source, $eventArgs)];"
					AlternationCount="{Binding Items.Count, RelativeSource={RelativeSource Self}}"
					ItemsSource="{Binding ViewModelProvider}"
					SelectedItem="{Binding SelectedItem, UpdateSourceTrigger=PropertyChanged}"
					Style="{StaticResource CustomDataGridStyle}"
					Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
					<DataGrid.CellStyle>
						<Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
							<Setter Property="Padding" Value="5" />
							<Style.Triggers>
								<Trigger Property="IsSelected" Value="True">
									<Setter Property="BorderBrush" Value="Transparent" />
								</Trigger>
							</Style.Triggers>
						</Style>
					</DataGrid.CellStyle>
					<DataGrid.Columns>
						<DataGridTemplateColumn>
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<CheckBox
										x:Name="CheckBoxItem"
										Margin="10"
										IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
										IsEnabled="{Binding Level, Converter={StaticResource UserLevelAllowConverter}}"
										Style="{StaticResource MaterialDesignCheckBox}">
										<cal:Message.Attach>
											[Event Click] = [Action OnClickCheckBoxItem($source, $eventArgs)];
										</cal:Message.Attach>
									</CheckBox>
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>

							<DataGridTemplateColumn.Header>
								<Border Margin="-2,0,0,0" Padding="0,0,0,0">
									<CheckBox
										x:Name="CheckBoxColumnHeader"
										HorizontalAlignment="Center"
										IsChecked="{Binding Data.IsCheckedCheckBoxColumnHeader, Mode=TwoWay, Source={StaticResource proxy}}"
										Style="{StaticResource MaterialDesignCheckBox}">
										<cal:Message.Attach>
											[Event Click] = [Action OnClickCheckBoxColumnHeader($source, $eventArgs)];
										</cal:Message.Attach>
									</CheckBox>
								</Border>
							</DataGridTemplateColumn.Header>
						</DataGridTemplateColumn>

						<!--  No  -->
						<DataGridTextColumn
							Binding="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=DataGridRow}}"
							Header="No"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>


						<!--  Image  -->
						<DataGridTemplateColumn Header="사진" IsReadOnly="True">
							<DataGridColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridColumn.HeaderStyle>
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<Image
										Width="50"
										Height="50"
										Margin="-5"
										HorizontalAlignment="Center"
										VerticalAlignment="Center"
										Source="{Binding Image, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource ImageConverter}, ConverterParameter='Full'}"
										Stretch="Uniform">
										<Image.ToolTip>
											<ToolTip Placement="Bottom">
												<ToolTip.Template>
													<ControlTemplate>
														<Border
															Background="#88000000"
															BorderBrush="DimGray"
															BorderThickness="1"
															CornerRadius="10">
															<Image
																Height="300"
																Margin="10"
																Source="{Binding Image, Converter={StaticResource ImageConverter}, ConverterParameter='Full'}" />
														</Border>
													</ControlTemplate>
												</ToolTip.Template>
											</ToolTip>
										</Image.ToolTip>
									</Image>
								</DataTemplate>

							</DataGridTemplateColumn.CellTemplate>

						</DataGridTemplateColumn>

						<!--  Level  -->
						<DataGridTextColumn
							Binding="{Binding Level}"
							Header="구분"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  ID  -->
						<DataGridTextColumn
							Binding="{Binding Username}"
							Header="아이디"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  Name  -->
						<DataGridTextColumn
							Binding="{Binding Name}"
							Header="성명"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  EmployeeNumber  -->
						<DataGridTextColumn
							Binding="{Binding EmployeeNumber}"
							Header="사번"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  Birth  -->
						<DataGridTextColumn
							Binding="{Binding Birth, StringFormat={}{0:yyyy-MM-dd}}"
							Header="생년월일"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  Phone  -->
						<DataGridTextColumn
							Binding="{Binding Phone}"
							Header="전화번호"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  Address  -->
						<DataGridTextColumn
							Binding="{Binding Address}"
							Header="주소"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  Email  -->
						<DataGridTextColumn
							Binding="{Binding EMail}"
							Header="이메일"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  Position  -->
						<DataGridTextColumn
							Binding="{Binding Position}"
							Header="직급"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  Department  -->
						<DataGridTextColumn
							Binding="{Binding Department}"
							Header="부서"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  Company  -->
						<DataGridTextColumn
							Binding="{Binding Company}"
							Header="회사"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

						<!--  Used  -->
						<DataGridTextColumn
							Width="150"
							Binding="{Binding Used, UpdateSourceTrigger=PropertyChanged}"
							Header="상태"
							IsReadOnly="True">
							<DataGridTextColumn.HeaderStyle>
								<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.HeaderStyle>
							<DataGridTextColumn.ElementStyle>
								<Style TargetType="{x:Type TextBlock}">
									<Setter Property="HorizontalAlignment" Value="Center" />
									<Setter Property="VerticalAlignment" Value="Center" />
								</Style>
							</DataGridTextColumn.ElementStyle>
						</DataGridTextColumn>

					</DataGrid.Columns>
				</DataGrid>
				<StackPanel
					Grid.Row="2"
					Margin="0,10,0,0"
					Orientation="Horizontal">

					<TextBlock Text="선택된 항목 : " />
					<TextBlock Text="{Binding SelectedItemCount, UpdateSourceTrigger=PropertyChanged}" />
				</StackPanel>
				<Separator
					Grid.Row="3"
					Background="#33000000"
					Style="{StaticResource MaterialDesignLightSeparator}" />
				<Grid Grid.Row="4" Margin="0,10,0,0">
					<Button
						x:Name="ClickCancel"
						VerticalAlignment="Center"
						IsDefault="True"
						Style="{StaticResource MaterialDesignFlatDarkBgButton}">
						<Button.Content>
							<StackPanel Orientation="Horizontal">
								<md:PackIcon Kind="Cancel" Style="{StaticResource ButtonIcon}" />
								<TextBlock
									Style="{StaticResource ButtonTextBlock}"
									Text="닫기"
									TextAlignment="Center" />
							</StackPanel>
						</Button.Content>
					</Button>
				</Grid>
			</Grid>
		</md:Card>
	</md:DialogHost>
</UserControl>

