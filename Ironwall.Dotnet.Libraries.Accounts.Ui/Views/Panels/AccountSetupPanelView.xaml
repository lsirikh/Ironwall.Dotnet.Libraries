<UserControl
	x:Class="Ironwall.Dotnet.Libraries.Accounts.Ui.Views.Panels.AccountSetupPanelView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behavior="clr-namespace:Ironwall.Dotnet.Libraries.Utils.Behaviors;assembly=Ironwall.Dotnet.Libraries.Utils"
	xmlns:cal="http://caliburnmicro.com"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:enum="clr-namespace:Ironwall.Dotnet.Libraries.Enums;assembly=Ironwall.Dotnet.Libraries.Enums"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:local="clr-namespace:Ironwall.Dotnet.Libraries.Accounts.Ui.Views.Panels"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:sys="clr-namespace:System;assembly=mscorlib"
	xmlns:utilPass="clr-namespace:Ironwall.Dotnet.Libraries.Utils.PasswordControl;assembly=Ironwall.Dotnet.Libraries.Utils"
	xmlns:utils="clr-namespace:Ironwall.Dotnet.Libraries.Utils;assembly=Ironwall.Dotnet.Libraries.Utils"
	d:DesignHeight="450"
	d:DesignWidth="800"
	FontFamily="{DynamicResource MaterialDesignFont}"
	mc:Ignorable="d">
	<ScrollViewer>
		<StackPanel Margin="15,0,10,0" Orientation="Vertical">
			<Grid Margin="15,0,10,0">
				<Grid>
					<Grid Height="300">
						<!--  ProgressBar section  -->
						<ProgressBar
							Width="100"
							Height="20"
							IsIndeterminate="True">
							<ProgressBar.Style>
                                <Style BasedOn="{StaticResource MaterialDesignCircularProgressBar}" TargetType="ProgressBar">
									<!--  기본값은 표시 안 함  -->
									<Setter Property="Visibility" Value="Collapsed" />
									<Style.Triggers>
										<!--  If IsVisible == false, show ProgressBar  -->
										<DataTrigger Binding="{Binding IsVisible}" Value="False">
											<Setter Property="Visibility" Value="Visible" />
										</DataTrigger>
									</Style.Triggers>
								</Style>
							</ProgressBar.Style>
						</ProgressBar>
					</Grid>
					<Grid>
						<Grid.Style>
							<Style TargetType="Grid">
								<Setter Property="Visibility" Value="Hidden" />
								<Style.Triggers>
									<DataTrigger Binding="{Binding IsVisible}" Value="True">
										<Setter Property="Visibility" Value="Visible" />
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</Grid.Style>
						<md:TransitioningContent>
							<md:TransitioningContent.OpeningEffects>
								<md:TransitionEffect Kind="FadeIn" Duration="0:0:0.3" />
							</md:TransitioningContent.OpeningEffects>
							<Grid Margin="16,24,24,0" IsEnabled="{Binding LoginViewModel.Level, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource AdminLevelAllowConverter}}">
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto" />
									<RowDefinition Height="Auto" />
								</Grid.RowDefinitions>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="16" />
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="Auto" />
								</Grid.ColumnDefinitions>

								<Grid.Resources>
									<Style TargetType="TextBlock">
										<Setter Property="FontFamily" Value="{StaticResource NotoSansCJKkRRegular}" />
										<Setter Property="FontSize" Value="16" />
										<Setter Property="Opacity" Value="0.75" />
									</Style>
								</Grid.Resources>

								<md:PackIcon
									Grid.Column="0"
									Width="18"
									Height="18"
									VerticalAlignment="Center"
									Foreground="{Binding ElementName=TextBlockSession, Path=Foreground}"
									Kind="ClockCheck" />

								<TextBlock
									x:Name="TextBlockSession"
									Grid.Column="2"
									IsEnabled="{Binding ElementName=ToggleButtonSessionTime, Path=IsChecked}"
									Text="세션 설정">

									<TextBlock.Style>
										<Style TargetType="TextBlock">
											<Setter Property="FontFamily" Value="{StaticResource NotoSansCJKkRRegular}" />
											<Setter Property="FontSize" Value="16" />
											<Setter Property="Opacity" Value="0.75" />

											<Style.Triggers>
												<Trigger Property="TextBlock.IsEnabled" Value="False">
													<Setter Property="TextBlock.Foreground" Value="Gray" />
												</Trigger>
											</Style.Triggers>
										</Style>
									</TextBlock.Style>
								</TextBlock>

								<ToggleButton
									x:Name="ToggleButtonSessionTime"
									Grid.Column="4"
									IsChecked="{Binding IsSession, Mode=TwoWay}"
									Style="{StaticResource MaterialDesignSwitchToggleButton}"
									ToolTip="MaterialDesignSwitchToggleButton" />

								<Grid
									Grid.Row="2"
									Grid.Column="2"
									Grid.ColumnSpan="9"
									Margin="0,16,0,0">
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto" />
										<ColumnDefinition Width="16" />
										<ColumnDefinition Width="*" />
									</Grid.ColumnDefinitions>

									<md:PackIcon
										Width="16"
										Height="16"
										VerticalAlignment="Center"
										Foreground="{Binding ElementName=TextBlockSession, Path=Foreground}"
										Kind="AlarmCheck" />

									<md:TimePicker
										x:Name="SessionTimePicker"
										Grid.Column="2"
										VerticalAlignment="Center"
										HorizontalContentAlignment="Right"
										md:HintAssist.FloatingScale="1"
										md:HintAssist.Hint="세션 기한 설정"
										md:HintAssist.IsFloating="True"
										Is24Hours="True"
										IsEnabled="{Binding ElementName=ToggleButtonSessionTime, Path=IsChecked}"
										Text="{Binding SessionExpiration, UpdateSourceTrigger=LostFocus, Mode=TwoWay, Converter={StaticResource TimeFormatConverter}}" />
								</Grid>
							</Grid>
						</md:TransitioningContent>
					</Grid>
				</Grid>
			</Grid>

		</StackPanel>
	</ScrollViewer>
</UserControl>

