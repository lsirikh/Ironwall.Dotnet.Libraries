<UserControl
	x:Class="Ironwall.Dotnet.Libraries.Gateway.Views.GatewaySetupView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behavior="clr-namespace:Ironwall.Dotnet.Libraries.Utils.Behaviors;assembly=Ironwall.Dotnet.Libraries.Utils"
	xmlns:behavior_inner="clr-namespace:Ironwall.Dotnet.Libraries.Gateway.Behaviors"
	xmlns:cal="http://caliburnmicro.com"
	xmlns:converter="clr-namespace:Ironwall.Dotnet.Libraries.Utils;assembly=Ironwall.Dotnet.Libraries.Utils"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:local="clr-namespace:Ironwall.Dotnet.Libraries.Gateway.Views"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:utils="clr-namespace:Ironwall.Dotnet.Libraries.Utils;assembly=Ironwall.Dotnet.Libraries.Utils"
	d:Background="WhiteSmoke"
	d:Height="600"
	d:Width="900"
	FontFamily="{DynamicResource MaterialDesignFont}"
	mc:Ignorable="d">

	<UserControl.Resources>
		<!--  뷰 전체 DataContext(ViewModel)를 상속받아서 Data 에 할당  -->
		<utils:BindingProxy x:Key="proxy" Data="{Binding}" />
		<converter:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
		<converter:BoolToInverseVisibleConverter x:Key="BoolToInverseVisibleConverter" />
	</UserControl.Resources>

	<Grid Margin="10,0">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="16" />
			<RowDefinition Height="200" />
			<RowDefinition Height="10" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<!--  상단 버튼  -->
		<StackPanel Grid.Row="0" Orientation="Vertical">
			<Grid Grid.Row="0" Margin="10,0">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="50" />
					<ColumnDefinition Width="1*" />
					<ColumnDefinition Width="1*" />
					<ColumnDefinition Width="70" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="70" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="70" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="70" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="70" />
					<ColumnDefinition Width="10" />
					<ColumnDefinition Width="70" />
				</Grid.ColumnDefinitions>
				<!--  Add Button  -->
				<Button
					Grid.Column="7"
					IsEnabled="{Binding IsButtonEnable}"
					Style="{StaticResource IconButtonStyle}"
					ToolTip="추가하기">
					<i:Interaction.Behaviors>
						<behavior:ButtonClickBehavior MethodName="OnClickInsertButton" />
					</i:Interaction.Behaviors>
					<md:PackIcon
						Width="30"
						Height="30"
						Kind="AddBox" />
				</Button>

				<!--  Remove Button  -->
				<Button
					Grid.Column="9"
					IsEnabled="{Binding IsButtonEnable}"
					Style="{StaticResource IconButtonStyle}"
					ToolTip="삭제하기">
					<i:Interaction.Behaviors>
						<behavior:ButtonClickBehavior MethodName="OnClickDeleteButton" />
					</i:Interaction.Behaviors>
					<md:PackIcon
						Width="30"
						Height="30"
						Kind="MinusBox" />
				</Button>

				<!--  Refresh Button  -->
				<Button
					Grid.Column="11"
					IsEnabled="{Binding IsButtonEnable}"
					Style="{StaticResource IconButtonStyle}"
					ToolTip="갱신하기">
					<i:Interaction.Behaviors>
						<behavior:ButtonClickBehavior MethodName="OnClickReloadButton" />
					</i:Interaction.Behaviors>
					<Button.Content>
						<Grid>
							<ProgressBar
								Width="30"
								Height="30"
								IsIndeterminate="True"
								Style="{DynamicResource MaterialDesignCircularProgressBar}"
								Visibility="{Binding ReloadButtonEnable, Converter={StaticResource BoolToInverseVisibleConverter}}" />
							<md:PackIcon
								Width="30"
								Height="30"
								Kind="CircleArrows"
								ToolTip="갱신하기"
								Visibility="{Binding ReloadButtonEnable, Converter={StaticResource BooleanToVisibilityConverter}}" />
						</Grid>
					</Button.Content>

				</Button>

				<!--  Save Button  -->
				<Button
					Grid.Column="13"
					Margin="2,2"
					VerticalAlignment="Center"
					md:RippleAssist.IsCentered="True"
					IsEnabled="{Binding IsButtonEnable}"
					Style="{StaticResource MaterialDesignIconButton}"
					ToolTip="저장하기">
					<i:Interaction.Behaviors>
						<behavior:ButtonClickBehavior MethodName="OnClickSaveButton" />
					</i:Interaction.Behaviors>
					<Button.Content>
						<Grid IsEnabled="{Binding IsButtonEnable}">
							<ProgressBar
								Width="30"
								Height="30"
								IsIndeterminate="True"
								Style="{DynamicResource MaterialDesignCircularProgressBar}"
								Visibility="{Binding SaveButtonEnable, Converter={StaticResource BoolToInverseVisibleConverter}}"
								Value="44" />
							<md:PackIcon
								Width="30"
								Height="30"
								Kind="Floppy"
								ToolTip="저장하기"
								Visibility="{Binding SaveButtonEnable, Converter={StaticResource BooleanToVisibilityConverter}}" />
						</Grid>
					</Button.Content>
				</Button>
			</Grid>
		</StackPanel>

		<!--  DataGrid 및 ProgressBar 영역  -->
		<Grid Grid.Row="2">
			<!--  ProgressBar (로딩 중)  -->
			<Grid Height="300">
				<ProgressBar
					Width="100"
					Height="20"
					IsIndeterminate="True">
					<ProgressBar.Style>
						<Style BasedOn="{StaticResource MaterialDesignLinearProgressBar}" TargetType="ProgressBar">
							<Setter Property="Visibility" Value="Collapsed" />
							<Style.Triggers>
								<DataTrigger Binding="{Binding ReloadButtonEnable}" Value="False">
									<Setter Property="Visibility" Value="Visible" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</ProgressBar.Style>
				</ProgressBar>
			</Grid>

			<!--  DataGrid  -->
			<DataGrid
				AutoGenerateColumns="False"
				CanUserAddRows="False"
				CanUserDeleteRows="False"
				ItemsSource="{Binding ViewModelProvider}"
				SelectionMode="Extended"
				Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
				<i:Interaction.Behaviors>
					<behavior:ClearSelectionOnEscBehavior />
					<behavior_inner:GatewayEventSelectedItemsBehavior SelectedItems="{Binding SelectedItems, Mode=TwoWay}" />
				</i:Interaction.Behaviors>

				<DataGrid.Columns>
					<!--  No (Index)  -->
					<DataGridTextColumn
						Width="60"
						Binding="{Binding Index}"
						Header="No"
						IsReadOnly="True">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock">
								<Setter Property="HorizontalAlignment" Value="Center" />
								<Setter Property="VerticalAlignment" Value="Center" />
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>

					<!--  ID  -->
					<!--<DataGridTextColumn
						Width="80"
						Binding="{Binding Id}"
						Header="ID"
						IsReadOnly="True">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock">
								<Setter Property="HorizontalAlignment" Value="Center" />
								<Setter Property="VerticalAlignment" Value="Center" />
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>-->

					<!--  이벤트명  -->
					<DataGridTemplateColumn Width="2*" Header="이벤트명">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<TextBlock
									HorizontalAlignment="Left"
									VerticalAlignment="Center"
									Text="{Binding EventName}" />
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
						<DataGridTemplateColumn.CellEditingTemplate>
							<DataTemplate>
								<TextBox VerticalAlignment="Center" Text="{Binding EventName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
							</DataTemplate>
						</DataGridTemplateColumn.CellEditingTemplate>
					</DataGridTemplateColumn>

					<!--  그룹  -->
					<DataGridTemplateColumn Width="100" Header="그룹">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<TextBlock
									HorizontalAlignment="Center"
									VerticalAlignment="Center"
									Text="{Binding Group}" />
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
						<DataGridTemplateColumn.CellEditingTemplate>
							<DataTemplate>
								<TextBox
									HorizontalAlignment="Center"
									VerticalAlignment="Center"
									Text="{Binding Group, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
									<i:Interaction.Behaviors>
										<behavior:NumericOnlyBehavior />
									</i:Interaction.Behaviors>
								</TextBox>
							</DataTemplate>
						</DataGridTemplateColumn.CellEditingTemplate>
					</DataGridTemplateColumn>

					<!--  사용 (CheckBox)  -->
					<DataGridTemplateColumn Width="100" Header="사용">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<CheckBox
									HorizontalAlignment="Center"
									VerticalAlignment="Center"
									IsChecked="{Binding IsEnable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>

					<!--  설명  -->
					<DataGridTemplateColumn Width="3*" Header="설명">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<TextBlock
									HorizontalAlignment="Left"
									VerticalAlignment="Center"
									Text="{Binding Description}"
									TextTrimming="CharacterEllipsis" />
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
						<DataGridTemplateColumn.CellEditingTemplate>
							<DataTemplate>
								<TextBox VerticalAlignment="Center" Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
							</DataTemplate>
						</DataGridTemplateColumn.CellEditingTemplate>
					</DataGridTemplateColumn>

				</DataGrid.Columns>
			</DataGrid>
		</Grid>

		<!--  하단 상태 영역  -->
		<Border
			Grid.Row="4"
			Padding="10,8"
			Background="#F5F5F5"
			BorderBrush="#E0E0E0"
			BorderThickness="1"
			CornerRadius="4">
			<StackPanel Orientation="Horizontal">
				<TextBlock
					VerticalAlignment="Center"
					FontSize="14"
					FontWeight="Medium"
					Text="선택 개수 : " />
				<TextBlock
					Margin="5,0,0,0"
					VerticalAlignment="Center"
					FontSize="14"
					FontWeight="Bold"
					Foreground="#1976D2"
					Text="{Binding SelectedItemCount}" />
				<TextBlock
					Margin="3,0,0,0"
					VerticalAlignment="Center"
					FontSize="14"
					Text="개" />

				<TextBlock
					Margin="30,0,0,0"
					VerticalAlignment="Center"
					FontSize="14"
					FontWeight="Medium"
					Text="전체 개수 : " />
				<TextBlock
					Margin="5,0,0,0"
					VerticalAlignment="Center"
					FontSize="14"
					FontWeight="Bold"
					Foreground="#388E3C"
					Text="{Binding ViewModelProvider.Count}" />
				<TextBlock
					Margin="3,0,0,0"
					VerticalAlignment="Center"
					FontSize="14"
					Text="개" />
			</StackPanel>
		</Border>
	</Grid>
</UserControl>
