<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:enums="clr-namespace:Ironwall.Dotnet.Libraries.Enums;assembly=Ironwall.Dotnet.Libraries.Enums"
	xmlns:milreg="clr-namespace:Ironwall.Dotnet.Libraries.GMaps.Ui.GMapMilitary"
	xmlns:sys="clr-namespace:System;assembly=mscorlib"
	xmlns:utils="clr-namespace:Ironwall.Dotnet.Libraries.Utils;assembly=Ironwall.Dotnet.Libraries.Utils">


	<ResourceDictionary.MergedDictionaries>
		<ResourceDictionary Source="/Ironwall.Dotnet.Libraries.GMaps.Ui;component/Resources/Resources.xaml" />
	</ResourceDictionary.MergedDictionaries>
	<utils:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
	<utils:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter" />
	<utils:UnitSizeToSymbolConverter x:Key="UnitSizeToSymbolConverter" />

	<!--  소속 구분 라디오 버튼 스타일  -->
	<!--
	<Style x:Key="AffiliationRadioButtonStyle" TargetType="RadioButton">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="RadioButton">
					<Border
						x:Name="border"
						Padding="10,5"
						Background="{TemplateBinding Background}"
						BorderBrush="#FF4A90E2"
						BorderThickness="2"
						CornerRadius="5">
						<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsChecked" Value="True">
							<Setter TargetName="border" Property="Background" Value="#FF4A90E2" />
							<Setter Property="Foreground" Value="White" />
						</Trigger>
						<Trigger Property="IsChecked" Value="False">
							<Setter TargetName="border" Property="Background" Value="Transparent" />
							<Setter Property="Foreground" Value="#FF4A90E2" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	-->
	<!--  메인 컨트롤 스타일  -->
	<!--
	<Style TargetType="{x:Type milreg:GMapMilitarySymbolRegisterControl}">
		<Setter Property="Background" Value="#FF5A9BD4" />
		<Setter Property="Foreground" Value="White" />
		<Setter Property="FontFamily" Value="Malgun Gothic" />
		<Setter Property="FontSize" Value="12" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type milreg:GMapMilitarySymbolRegisterControl}">
					<Border
						MinWidth="700"
						MinHeight="500"
						Background="{TemplateBinding Background}"
						BorderBrush="#FF2E5B8A"
						BorderThickness="3"
						CornerRadius="10">
						<Grid Margin="20">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="*" />
								<RowDefinition Height="Auto" />
							</Grid.RowDefinitions>

							-->
	<!--  제목  -->
	<!--
							<Border
								Grid.Row="0"
								Margin="0,0,0,20"
								Padding="20,10"
								HorizontalAlignment="Center"
								Background="#FF4A90E2"
								CornerRadius="20">
								<TextBlock
									HorizontalAlignment="Center"
									FontSize="16"
									FontWeight="Bold"
									Foreground="White"
									Text="군대 부호 등록" />
							</Border>

							-->
	<!--  소속 선택  -->
	<!--
							<Grid Grid.Row="1" Margin="0,0,0,15">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="80" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>

								<TextBlock
									Grid.Column="0"
									VerticalAlignment="Center"
									FontWeight="Bold"
									Text="소속" />

								<UniformGrid
									Grid.Column="1"
									Columns="4"
									Rows="1">
									<RadioButton
										Margin="2"
										Content="알 수 없음"
										GroupName="Affiliation"
										IsChecked="{Binding Affiliation, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EnumMilitaryAffiliation.Unknown}}"
										Style="{StaticResource AffiliationRadioButtonStyle}" />
									<RadioButton
										Margin="2"
										Content="우군"
										GroupName="Affiliation"
										IsChecked="{Binding Affiliation, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EnumMilitaryAffiliation.Friend}}"
										Style="{StaticResource AffiliationRadioButtonStyle}" />
									<RadioButton
										Margin="2"
										Content="중립"
										GroupName="Affiliation"
										IsChecked="{Binding Affiliation, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EnumMilitaryAffiliation.Neutral}}"
										Style="{StaticResource AffiliationRadioButtonStyle}" />
									<RadioButton
										Margin="2"
										Content="적군"
										GroupName="Affiliation"
										IsChecked="{Binding Affiliation, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EnumMilitaryAffiliation.Hostile}}"
										Style="{StaticResource AffiliationRadioButtonStyle}" />
								</UniformGrid>
							</Grid>

							-->
	<!--  공중성 선택  -->
	<!--
							<Grid Grid.Row="2" Margin="0,0,0,15">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="80" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>

								<TextBlock
									Grid.Column="0"
									VerticalAlignment="Center"
									FontWeight="Bold"
									Text="공중성" />

								<UniformGrid
									Grid.Column="1"
									Columns="4"
									Rows="1">
									<RadioButton
										Margin="2"
										Content="공중"
										GroupName="BattleDimension"
										IsChecked="{Binding BattleDimension, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EnumMilitaryBattleDimension.Air}}"
										Style="{StaticResource AffiliationRadioButtonStyle}" />
									<RadioButton
										Margin="2"
										Content="지상"
										GroupName="BattleDimension"
										IsChecked="{Binding BattleDimension, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EnumMilitaryBattleDimension.Land}}"
										Style="{StaticResource AffiliationRadioButtonStyle}" />
									<RadioButton
										Margin="2"
										Content="해상"
										GroupName="BattleDimension"
										IsChecked="{Binding BattleDimension, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EnumMilitaryBattleDimension.Sea}}"
										Style="{StaticResource AffiliationRadioButtonStyle}" />
									<RadioButton
										Margin="2"
										Content="해저"
										GroupName="BattleDimension"
										IsChecked="{Binding BattleDimension, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:EnumMilitaryBattleDimension.Subsurface}}"
										Style="{StaticResource AffiliationRadioButtonStyle}" />
								</UniformGrid>
							</Grid>

							-->
	<!--  메인 컨텐츠 영역  -->
	<!--
							<Grid Grid.Row="3">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="200" />
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="250" />
								</Grid.ColumnDefinitions>

								-->
	<!--  좌측: 부대 심볼 리스트  -->
	<!--
								<Border
									Grid.Column="0"
									Margin="0,0,10,0"
									Background="#FF4A90E2"
									CornerRadius="5">
									<Grid>
										<Grid.RowDefinitions>
											<RowDefinition Height="Auto" />
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>

										<TextBlock
											Grid.Row="0"
											Margin="10"
											FontWeight="Bold"
											Foreground="White"
											Text="부대 심볼" />

										<ListBox
											Name="PART_UnitTypeList"
											Grid.Row="1"
											Margin="5"
											Background="Transparent"
											BorderThickness="0"
											Foreground="White"
											ItemsSource="{TemplateBinding AvailableUnitTypes}"
											SelectedItem="{Binding UnitType, RelativeSource={RelativeSource TemplatedParent}}">
											<ListBox.ItemTemplate>
												<DataTemplate>
													<TextBlock Padding="5" Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}" />
												</DataTemplate>
											</ListBox.ItemTemplate>
										</ListBox>
									</Grid>
								</Border>

								-->
	<!--  중앙: 미리보기  -->
	<!--
								<Border
									Grid.Column="1"
									Margin="5"
									Background="#FF6BA6CD"
									CornerRadius="5">
									<Grid>
										<Grid.RowDefinitions>
											<RowDefinition Height="Auto" />
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>

										-->
	<!--  부대 규모 표시 (XX)  -->
	<!--
										<TextBlock
											Name="PART_UnitSizeDisplay"
											Grid.Row="0"
											Margin="0,10,0,5"
											HorizontalAlignment="Center"
											FontSize="16"
											FontWeight="Bold"
											Foreground="White"
											Text="{Binding UnitSize, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource UnitSizeToSymbolConverter}}" />

										-->
	<!--  군사 심볼 미리보기 영역  -->
	<!--
										<ContentControl
											Name="PART_MilitarySymbolPreview"
											Grid.Row="1"
											Width="120"
											Height="120"
											HorizontalAlignment="Center"
											VerticalAlignment="Center"
											Content="{Binding RelativeSource={RelativeSource TemplatedParent}}"
											ContentTemplate="{StaticResource MilitaryMarkerPreviewTemplate}" />
									</Grid>
								</Border>

								-->
	<!--  우측: 속성 입력  -->
	<!--
								<Border
									Grid.Column="2"
									Margin="10,0,0,0"
									Background="#FF4A90E2"
									CornerRadius="5">
									<StackPanel Margin="15">
										-->
	<!--  규모 선택  -->
	<!--
										<TextBlock
											Margin="0,0,0,5"
											FontWeight="Bold"
											Text="규모" />
										<ComboBox
											Margin="0,0,0,15"
											ItemsSource="{TemplateBinding AvailableUnitSizes}"
											SelectedItem="{Binding UnitSize, RelativeSource={RelativeSource TemplatedParent}}">
											<ComboBox.ItemTemplate>
												<DataTemplate>
													<TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}" />
												</DataTemplate>
											</ComboBox.ItemTemplate>
										</ComboBox>

										-->
	<!--  부대 지시자  -->
	<!--
										<TextBlock
											Margin="0,0,0,5"
											FontWeight="Bold"
											Text="부대 지시자" />
										<TextBox Margin="0,0,0,15" Text="{Binding UnitDesignator, RelativeSource={RelativeSource TemplatedParent}}" />

										-->
	<!--  상급 부대명  -->
	<!--
										<TextBlock
											Margin="0,0,0,5"
											FontWeight="Bold"
											Text="상급 부대명" />
										<TextBox Margin="0,0,0,15" Text="{Binding HigherFormation, RelativeSource={RelativeSource TemplatedParent}}" />

										-->
	<!--  콜 사인  -->
	<!--
										<TextBlock
											Margin="0,0,0,5"
											FontWeight="Bold"
											Text="콜 사인" />
										<TextBox Margin="0,0,0,15" Text="{Binding CallSign, RelativeSource={RelativeSource TemplatedParent}}" />

										-->
	<!--  국가 코드  -->
	<!--
										<TextBlock
											Margin="0,0,0,5"
											FontWeight="Bold"
											Text="국가 코드" />
										<TextBox Margin="0,0,0,15" Text="{Binding CountryCode, RelativeSource={RelativeSource TemplatedParent}}" />

										-->
	<!--  계획 속성  -->
	<!--
										<TextBlock
											Margin="0,0,0,5"
											FontWeight="Bold"
											Text="계획 속성" />
										<ComboBox ItemsSource="{TemplateBinding AvailableStandardIdentities}" SelectedItem="{Binding StandardIdentity, RelativeSource={RelativeSource TemplatedParent}}">
											<ComboBox.ItemTemplate>
												<DataTemplate>
													<TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}" />
												</DataTemplate>
											</ComboBox.ItemTemplate>
										</ComboBox>
									</StackPanel>
								</Border>
							</Grid>

							-->
	<!--  하단 버튼  -->
	<!--
							<StackPanel
								Grid.Row="4"
								Margin="0,20,0,0"
								HorizontalAlignment="Center"
								Orientation="Horizontal">
								<Button
									Width="80"
									Height="35"
									Margin="0,0,10,0"
									Background="#FF7F8C8D"
									BorderThickness="0"
									Command="{TemplateBinding CancelCommand}"
									Content="취소"
									CornerRadius="5"
									Foreground="White" />
								<Button
									Width="80"
									Height="35"
									Background="#FF27AE60"
									BorderThickness="0"
									Command="{TemplateBinding RegisterCommand}"
									Content="등록"
									CornerRadius="5"
									Foreground="White" />
							</StackPanel>
						</Grid>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	-->
	<!--  군사 심볼 미리보기 템플릿 (MilitaryMarkerStyle 연결점)  -->
	<!--
	<DataTemplate x:Key="MilitaryMarkerPreviewTemplate">
		<ContentControl Content="{Binding}" ContentTemplate="{StaticResource MilitarySymbolTemplate}" />
	</DataTemplate>-->

</ResourceDictionary>