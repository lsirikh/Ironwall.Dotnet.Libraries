<Window x:Class="Ironwall.Dotnet.Libraries.GMaps.Ui.Views.Dialogs.BroadcastPlayDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="음원 실행" Width="320" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- FileGroupId -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="파일 그룹 ID:" Width="100" VerticalAlignment="Center"/>
            <TextBox Width="160" Text="{Binding FileGroupId, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- Repeat -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="반복 횟수:" Width="100" VerticalAlignment="Center"/>
            <TextBox Width="160" Text="{Binding Repeat, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="전송" Width="72" Margin="0,0,8,0" IsDefault="True"
                    Click="OnSend_Click"/>
            <Button Content="취소" Width="72" IsCancel="True"
                    Click="OnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
