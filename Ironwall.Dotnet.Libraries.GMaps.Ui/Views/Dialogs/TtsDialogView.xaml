<Window x:Class="Ironwall.Dotnet.Libraries.GMaps.Ui.Views.Dialogs.TtsDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TTS 실행" Width="360" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Message -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="방송 메시지:" Width="100" VerticalAlignment="Center"/>
            <TextBox Width="200" Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="False" MaxLength="200"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="전송" Width="72" Margin="0,0,8,0" IsDefault="True"
                    Click="OnSend_Click"/>
            <Button Content="취소" Width="72" IsCancel="True"
                    Click="OnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
