<UserControl
	x:Class="Ironwall.Dotnet.Libraries.Devices.Ui.Views.DeviceGroupSelectionView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behavior="clr-namespace:Ironwall.Dotnet.Libraries.Utils.Behaviors;assembly=Ironwall.Dotnet.Libraries.Utils"
	xmlns:cal="http://caliburnmicro.com"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:behavior_inner="clr-namespace:Ironwall.Dotnet.Libraries.Devices.Ui.Behaviors"
	xmlns:local="clr-namespace:Ironwall.Dotnet.Libraries.Devices.Ui.Views"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:utils="clr-namespace:Ironwall.Dotnet.Libraries.Utils;assembly=Ironwall.Dotnet.Libraries.Utils"
	d:Background="WhiteSmoke"
	mc:Ignorable="d">

	<GroupBox
		Margin="5"
		Style="{StaticResource MaterialDesignGroupBox}"
		UseLayoutRounding="True">
		<GroupBox.Header>
			<StackPanel HorizontalAlignment="Stretch" Orientation="Horizontal">
				<md:PackIcon Kind="Group" />
				<TextBlock
					Margin="5,0"
					VerticalAlignment="Center"
					Text="장비그룹 속성" />
			</StackPanel>
		</GroupBox.Header>

		<Grid Margin="5,4,5,4">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<!--  ===== Row 0: Name / Desc / 적용  |  추가 / 제거 =====  -->
			<Grid Grid.Row="0" Margin="0,0,0,6">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="1.5*" MinWidth="60" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="2*" MinWidth="80" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<TextBlock
					Grid.Column="0"
					Style="{StaticResource TextBlockTitle}"
					Text="Name" />
				<TextBox
					Grid.Column="1"
					md:HintAssist.Hint="1구역"
					Style="{StaticResource TextBoxInput}"
					Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

				<TextBlock
					Grid.Column="2"
					Style="{StaticResource TextBlockTitle}"
					Text="Description" />
				<TextBox
					Grid.Column="3"
					md:HintAssist.Hint="설명"
					Style="{StaticResource TextBoxInput}"
					Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

				<!--  적용 버튼  -->
				<Button
					x:Name="ApplyButton"
					Grid.Column="4"
					HorizontalAlignment="Center"
					Margin="5 0"
					BorderBrush="{DynamicResource PrimaryHueDarkBrush}"
					Style="{StaticResource MaterialDesignFlatButton}">
					<StackPanel Orientation="Horizontal">
						<md:PackIcon Kind="Check" Style="{StaticResource ButtonIcon}" />
						<TextBlock Style="{StaticResource ButtonTextBlock}" Text="적용" />
					</StackPanel>
				</Button>

				<!--  Spacer + vertical divider  -->
				<Rectangle
					Grid.Column="5"
					Width="1"
					Margin="0,4"
					HorizontalAlignment="Right"
					VerticalAlignment="Stretch"
					Fill="{DynamicResource MaterialDesignDivider}" />

				<!--  추가 버튼  -->
				<Button
					Grid.Column="6"
					IsEnabled="{Binding CanAddAssign}"
					Style="{StaticResource IconButtonStyle}"
					ToolTip="추가하기">
					<i:Interaction.Behaviors>
						<behavior:ButtonClickBehavior MethodName="AddDeviceButton" />
					</i:Interaction.Behaviors>
					<Button.Content>
						<Grid>
							<ProgressBar
								Width="30"
								Height="30"
								IsIndeterminate="True"
								Style="{DynamicResource MaterialDesignCircularProgressBar}"
								Visibility="{Binding AddButtonEnable, Converter={StaticResource BoolToInverseVisibleConverter}}" />
							<md:PackIcon
								Width="30"
								Height="30"
								Kind="AddBox"
								Visibility="{Binding AddButtonEnable, Converter={StaticResource BooleanToVisibilityConverter}}" />
						</Grid>
					</Button.Content>
				</Button>

				<!--  제거 버튼  -->
				<Button
					Grid.Column="7"
					IsEnabled="{Binding CanRemoveDeviceButton}"
					Style="{StaticResource IconButtonStyle}"
					ToolTip="삭제하기">
					<i:Interaction.Behaviors>
						<behavior:ButtonClickBehavior MethodName="RemoveDeviceButton" />
					</i:Interaction.Behaviors>
					<Button.Content>
						<Grid>
							<ProgressBar
								Width="30"
								Height="30"
								IsIndeterminate="True"
								Style="{DynamicResource MaterialDesignCircularProgressBar}"
								Visibility="{Binding RemoveButtonEnable, Converter={StaticResource BoolToInverseVisibleConverter}}" />
							<md:PackIcon
								Width="30"
								Height="30"
								Kind="MinusBox"
								Visibility="{Binding RemoveButtonEnable, Converter={StaticResource BooleanToVisibilityConverter}}" />
						</Grid>
					</Button.Content>
				</Button>

				<!--  새로고침 버튼  -->
				<Button
					Grid.Column="8"
					IsEnabled="{Binding IsSingleSelected}"
					Style="{StaticResource IconButtonStyle}"
					ToolTip="갱신하기">
					<i:Interaction.Behaviors>
						<behavior:ButtonClickBehavior MethodName="RefreshButton" />
					</i:Interaction.Behaviors>
					<Button.Content>
						<Grid>
							<ProgressBar
								Width="30"
								Height="30"
								IsIndeterminate="True"
								Style="{DynamicResource MaterialDesignCircularProgressBar}"
								Visibility="{Binding ReloadButtonEnable, Converter={StaticResource BoolToInverseVisibleConverter}}" />
							<md:PackIcon
								Width="30"
								Height="30"
								Kind="CircleArrows"
								Visibility="{Binding ReloadButtonEnable, Converter={StaticResource BooleanToVisibilityConverter}}" />
						</Grid>
					</Button.Content>
				</Button>
			</Grid>

			<!--  ===== Row 1: 소속 장비 DataGrid (단일 선택 시만 표시) =====  -->
			<Grid
				Grid.Row="1"
				Visibility="{Binding IsSingleSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
				<ProgressBar
					Width="50"
					Height="50"
					Margin="16"
					IsIndeterminate="True"
					Style="{DynamicResource MaterialDesignCircularProgressBar}"
					Visibility="{Binding IsVisible, Converter={StaticResource BoolToInverseVisibleConverter}}"
					Value="44" />
				<DataGrid
					AutoGenerateColumns="False"
					CanUserAddRows="False"
					CanUserDeleteRows="False"
					IsReadOnly="True"
					ItemsSource="{Binding AssignedDevices}"
					MinHeight="110"
					SelectionMode="Extended"
					Style="{StaticResource MaterialDesignDataGrid}"
					Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
					<i:Interaction.Behaviors>
						<behavior:ClearSelectionOnEscBehavior />
						<behavior_inner:DeviceAssignSelectedItemsBehavior
							SelectedItems="{Binding SelectedAssignedDevices, Mode=OneWay}" />
					</i:Interaction.Behaviors>
					<DataGrid.Columns>
						<DataGridTextColumn
							Binding="{Binding DeviceNumber}"
							Header="번호"
							IsReadOnly="True"
							Width="60" />
						<DataGridTextColumn
							Binding="{Binding DeviceName}"
							Header="장비명"
							IsReadOnly="True"
							Width="3*" />
						<DataGridTextColumn
							Binding="{Binding DeviceType}"
							Header="장비유형"
							IsReadOnly="True"
							Width="1*" />
						<DataGridTextColumn
							Binding="{Binding Status}"
							Header="상태"
							IsReadOnly="True"
							Width="1*" />
						<DataGridCheckBoxColumn
							Binding="{Binding IsEnable}"
							Header="활성화"
							IsReadOnly="True"
							Width="1*" />
					</DataGrid.Columns>
				</DataGrid>
			</Grid>
		</Grid>
	</GroupBox>
</UserControl>
