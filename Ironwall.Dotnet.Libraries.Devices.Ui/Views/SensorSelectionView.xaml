<UserControl
	x:Class="Ironwall.Dotnet.Libraries.Devices.Ui.Views.SensorSelectionView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behavior="clr-namespace:Ironwall.Dotnet.Libraries.Utils.Behaviors;assembly=Ironwall.Dotnet.Libraries.Utils"
	xmlns:cal="http://caliburnmicro.com"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:enums="clr-namespace:Ironwall.Dotnet.Libraries.Enums;assembly=Ironwall.Dotnet.Libraries.Enums"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:local="clr-namespace:Ironwall.Dotnet.Libraries.Devices.Ui.Views"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:sys="clr-namespace:System;assembly=mscorlib"
	xmlns:utilPass="clr-namespace:Ironwall.Dotnet.Libraries.Utils.PasswordControl;assembly=Ironwall.Dotnet.Libraries.Utils"
	xmlns:utils="clr-namespace:Ironwall.Dotnet.Libraries.Utils;assembly=Ironwall.Dotnet.Libraries.Utils"
	d:Background="WhiteSmoke"
	mc:Ignorable="d">
	<UserControl.Resources>
		<!--  뷰 전체 DataContext(ViewModel)를 상속받아서 Data 에 할당  -->
		<utils:BindingProxy x:Key="proxy" Data="{Binding}" />
	</UserControl.Resources>

	<GroupBox
		Margin="5"
		Style="{StaticResource MaterialDesignGroupBox}"
		UseLayoutRounding="True">
		<GroupBox.Header>
			<StackPanel HorizontalAlignment="Stretch" Orientation="Horizontal">
				<md:PackIcon Kind="Settings" />
				<TextBlock
					Margin="5,0"
					VerticalAlignment="Center"
					Text="센서 속성" />
			</StackPanel>
		</GroupBox.Header>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="1*" />
			</Grid.RowDefinitions>
			<Grid Grid.Row="0" Margin="5,0">
				<!--  5 쌍(라벨+입력) × 2행  -->
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<!--  label / editor 한 쌍 = 2col × 5쌍 = 10col  -->
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="2*" MinWidth="50" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="2*" MinWidth="50" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="2*" MinWidth="50" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="2*" MinWidth="50" />
				</Grid.ColumnDefinitions>

				<!--  ───────────── ① DeviceNumber ─────────────  -->
				<TextBlock
					Grid.Row="0"
					Grid.Column="0"
					Style="{StaticResource TextBlockTitle}"
					Text="Device No" />
				<TextBox
					Grid.Row="0"
					Grid.Column="1"
					md:HintAssist.Hint="1"
					Style="{StaticResource TextBoxInput}"
					Text="{Binding DeviceNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

				<!--  ───────────── ② DeviceGroup ─────────────  -->
				<TextBlock
					Grid.Row="0"
					Grid.Column="2"
					Style="{StaticResource TextBlockTitle}"
					Text="Group" />
				<TextBox
					Grid.Row="0"
					Grid.Column="3"
					md:HintAssist.Hint="100"
					Style="{StaticResource TextBoxInput}"
					Text="{Binding DeviceGroup, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

				<!--  ───────────── ③ DeviceName ─────────────  -->
				<TextBlock
					Grid.Row="0"
					Grid.Column="4"
					Style="{StaticResource TextBlockTitle}"
					Text="Name" />
				<TextBox
					Grid.Row="0"
					Grid.Column="5"
					md:HintAssist.Hint="제어기_01 / 펜스센서_001"
					Style="{StaticResource TextBoxInput}"
					Text="{Binding DeviceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

				<!--  ───────────── ④ Controller ─────────────  -->
				<TextBlock
					Grid.Row="0"
					Grid.Column="6"
					Style="{StaticResource TextBlockTitle}"
					Text="Controller" />
				<ComboBox
					Grid.Row="0"
					Grid.Column="7"
					ItemsSource="{Binding Data.Controllers, Source={StaticResource proxy}, UpdateSourceTrigger=PropertyChanged}"
					SelectedItem="{Binding Controller, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
					Style="{StaticResource ComboBoxContent}">
					<ComboBox.ItemsPanel>
						<ItemsPanelTemplate>
							<VirtualizingStackPanel />
						</ItemsPanelTemplate>
					</ComboBox.ItemsPanel>
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding DeviceName}" TextAlignment="Center" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
					<ComboBox.ItemContainerStyle>
						<Style TargetType="{x:Type ComboBoxItem}" />
					</ComboBox.ItemContainerStyle>
				</ComboBox>

				<!--  ========== 두 번째 행 =================================================  -->

				<!--  ───────────── ⑥ DeviceType ─────────────  -->
				<TextBlock
					Grid.Row="1"
					Grid.Column="0"
					Style="{StaticResource TextBlockTitle}"
					Text="Type" />
				<ComboBox
					Grid.Row="1"
					Grid.Column="1"
					ItemsSource="{Binding Source={utils:EnumBindingSource {x:Type enums:EnumDeviceType}}}"
					SelectedItem="{Binding DeviceType, Mode=TwoWay}"
					Style="{StaticResource ComboBoxContent}">
					<ComboBox.ItemsPanel>
						<ItemsPanelTemplate>
							<VirtualizingStackPanel />
						</ItemsPanelTemplate>
					</ComboBox.ItemsPanel>
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>

				<!--  ───────────── ⑦ Version ─────────────  -->
				<TextBlock
					Grid.Row="1"
					Grid.Column="2"
					Style="{StaticResource TextBlockTitle}"
					Text="Ver." />
				<TextBox
					Grid.Row="1"
					Grid.Column="3"
					md:HintAssist.Hint="v1.0"
					Style="{StaticResource TextBoxInput}"
					Text="{Binding Version, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

				<!--  ───────────── ⑧ Status ─────────────  -->
				<TextBlock
					Grid.Row="1"
					Grid.Column="4"
					Style="{StaticResource TextBlockTitle}"
					Text="Status" />
				<ComboBox
					Grid.Row="1"
					Grid.Column="5"
					ItemsSource="{Binding Source={utils:EnumBindingSource {x:Type enums:EnumDeviceStatus}}}"
					SelectedItem="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
					Style="{StaticResource ComboBoxContent}">
					<ComboBox.ItemsPanel>
						<ItemsPanelTemplate>
							<VirtualizingStackPanel />
						</ItemsPanelTemplate>
					</ComboBox.ItemsPanel>
					<ComboBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>

				<!--  Button Group  -->
				<Button
					x:Name="ApplyButton"
					Grid.Row="2"
					Grid.Column="8"
					Grid.ColumnSpan="3"
					BorderBrush="{DynamicResource PrimaryHueDarkBrush}"
					Style="{StaticResource MaterialDesignFlatButton}">
					<Button.Content>
						<StackPanel Orientation="Horizontal">
							<md:PackIcon Kind="Check" Style="{StaticResource ButtonIcon}" />
							<TextBlock Style="{StaticResource ButtonTextBlock}" Text="적용" />
						</StackPanel>
					</Button.Content>
				</Button>
			</Grid>

		</Grid>
	</GroupBox>


</UserControl>