<UserControl
    x:Class="Ironwall.Dotnet.Libraries.Reports.Ui.Views.Panels.ReportTemplateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cal="http://caliburnmicro.com"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 툴바 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="Create" Content="새 템플릿" MinWidth="90" Style="{StaticResource ModernPrimaryButton}" />
            <Button x:Name="LoadAsync" Content="새로고침" MinWidth="84" Margin="8,0,0,0" Style="{StaticResource ModernSecondaryButton}" />
            <Button x:Name="Edit" Content="수정" MinWidth="72" Margin="8,0,0,0"
                    Style="{StaticResource ModernSecondaryButton}" IsEnabled="{Binding CanEdit}" />
            <Button x:Name="Delete" Content="삭제" MinWidth="72" Margin="8,0,0,0"
                    Style="{StaticResource ModernDangerButton}" IsEnabled="{Binding CanDelete}" />
        </StackPanel>

        <!-- 목록 -->
        <Border Grid.Row="1" Style="{StaticResource StandardPanelCard}">
            <DataGrid x:Name="Items" AutoGenerateColumns="False" IsReadOnly="True"
                      SelectedItem="{Binding SelectedItem}" CanUserAddRows="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      RowHeight="38" FontSize="13"
                      Style="{StaticResource ModernDataGrid}"
                      ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                      CellStyle="{StaticResource ModernDataGridCell}"
                      RowStyle="{StaticResource ModernDataGridRow}"
                      BorderThickness="0" Background="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="52" />
                    <DataGridTextColumn Header="이름" Binding="{Binding Name}" Width="200" />
                    <DataGridTextColumn Header="설명" Binding="{Binding Description}" Width="*" MinWidth="200" />
                    <DataGridTextColumn Header="유형" Binding="{Binding ReportType}" Width="90" />
                    <DataGridCheckBoxColumn Header="공개" Binding="{Binding IsPublic}" Width="60" />
                    <DataGridTextColumn Header="기본기간" Binding="{Binding DefaultPeriod}" Width="80" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- 빈 상태 / 연결 실패 -->
        <TextBlock Grid.Row="1" Text="{Binding EmptyStateText}" HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextWrapping="Wrap" TextAlignment="Center" MaxWidth="420"
                   Foreground="{DynamicResource TextMutedBrush}"
                   Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- 로딩 스피너 — 새로고침 중(IsBusy) DataGrid 위 중앙. 목록 탭과 동일 패턴 -->
        <ProgressBar Grid.Row="1" Style="{DynamicResource MaterialDesignCircularProgressBar}" IsIndeterminate="True"
                     Width="44" Height="44" HorizontalAlignment="Center" VerticalAlignment="Center"
                     Foreground="{DynamicResource PrimaryBrush}"
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</UserControl>
